PrivateType is a private, local push-to-talk dictation app for Windows. Hold a shortcut, speak, and release it to insert text into the field where dictation began—without cloud recognition or transcript history.

## Highlights

### Reliable model loading

- Fixed a race that could incorrectly report that the local model failed to load after quickly pressing the dictation shortcut a second time.
- Repeated shortcut presses now share the same pending model-load operation.
- PrivateType only reports the model as loaded after the local recognition engine confirms that it is ready.

### Easier portable download

- The download and checksum now include the app version in their filenames.
- The extracted folder is versioned too.
- A clearly visible `PrivateType.exe` launcher and `README - Start here.txt` now sit at the top level.
- Technical .NET runtime, speech-engine, license, model, and settings files are organized under `app`.

## Download and start

1. Download `PrivateType-1.0.2-win-x64.zip` and `PrivateType-1.0.2-win-x64.zip.sha256` below.
2. Verify the SHA-256 checksum.
3. Extract the complete ZIP into a writable folder.
4. Open `PrivateType 1.0.2` and double-click `PrivateType.exe`.
5. Approve the separate local speech-model download on first launch.

The model is not bundled in the ZIP. PrivateType downloads it directly from NVIDIA on Hugging Face and verifies its pinned size and SHA-256 hash before use.

## Requirements

- 64-bit Windows 10 or Windows 11
- A modern x64 CPU and at least 4 GB RAM; 8 GB or more is recommended
- About 2 GB free disk space for the portable app, model, and working room
- [Microsoft Visual C++ Redistributable for Visual Studio 2015–2022 (x64)](https://aka.ms/vs/17/release/vc_redist.x64.exe)

## Privacy

Speech recognition runs locally. PrivateType has no account, cloud-recognition fallback, telemetry, retained audio, or transcript history. Review diagnostic reports before sharing them because application diagnostics can still contain information about your computer environment.

## Full changelog

See all changes since the previous draft: [v1.0.0...v1.0.2](https://github.com/kkolodziejczak/PrivateType/compare/v1.0.0...v1.0.2).
