<Window x:Class="PrivateType.App.ModelSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set up PrivateType" Icon="/PrivateType;component/Assets/PrivateType.ready.ico" Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Foreground="{DynamicResource ColorText}" FontFamily="{DynamicResource AppFont}">
  <Window.Resources>
    <Style TargetType="CheckBox"><Setter Property="Foreground" Value="{DynamicResource ColorText}"/><Setter Property="Cursor" Value="Hand"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="CheckBox"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="22"/><ColumnDefinition Width="10"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><Border x:Name="Box" Width="22" Height="22" VerticalAlignment="Center" Background="{DynamicResource ColorBg}" BorderBrush="{DynamicResource ColorAccent}" BorderThickness="2" CornerRadius="4"><Path x:Name="Mark" Data="M3,9 L7,13 L15,4" Stroke="{DynamicResource ColorAccent100}" StrokeThickness="2" Visibility="Collapsed"/></Border><ContentPresenter Grid.Column="2" VerticalAlignment="Center" RecognizesAccessKey="True"/></Grid><ControlTemplate.Triggers><Trigger Property="IsChecked" Value="True"><Setter TargetName="Box" Property="Background" Value="{DynamicResource ColorAccent800}"/><Setter TargetName="Mark" Property="Visibility" Value="Visible"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style>
    <Style TargetType="ProgressBar">
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ProgressBar"><Border x:Name="PART_Track" Background="{DynamicResource ColorNeutral800}" CornerRadius="4"><Border x:Name="PART_Indicator" HorizontalAlignment="Left" Background="{DynamicResource ColorAccent}" CornerRadius="4"/></Border></ControlTemplate></Setter.Value></Setter>
    </Style>
  </Window.Resources>
  <Border Margin="8" Background="{DynamicResource ColorSurface}" BorderBrush="{DynamicResource ColorDivider}" BorderThickness="1" CornerRadius="16">
    <Grid>
      <Grid.RowDefinitions><RowDefinition Height="42"/><RowDefinition Height="*"/></Grid.RowDefinitions>
      <Border Background="{DynamicResource ColorNeutral900}" CornerRadius="15,15,0,0" MouseLeftButtonDown="DragWindow">
        <Grid Margin="16,0,8,0">
          <TextBlock VerticalAlignment="Center" Opacity=".8" FontSize="13" Text="PrivateType — local model setup"/>
          <Button Width="28" Height="28" Padding="0" HorizontalAlignment="Right" Content="×" FontSize="18" Background="Transparent" BorderBrush="Transparent" Foreground="{DynamicResource ColorNeutral400}" Click="CloseWindow"/>
        </Grid>
      </Border>
      <Border Grid.Row="1" Padding="22" Background="{DynamicResource ColorSurface}" CornerRadius="0,0,14,14">
        <StackPanel>
          <TextBlock x:Name="HeadingText" FontSize="22" FontWeight="SemiBold" Text="Download local dictation"/>
          <TextBlock x:Name="StatusText" Margin="0,6,0,12" Opacity=".75" FontSize="14" TextWrapping="Wrap" Text="Download the local speech model before using dictation."/>
          <StackPanel x:Name="ConsentPanel">
            <Border Padding="12" Background="{DynamicResource ColorBg}" BorderBrush="{DynamicResource ColorDivider}" BorderThickness="1" CornerRadius="8">
              <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="Nemotron 3.5 ASR Streaming 0.6B (Q8_0)"/>
                <TextBlock Margin="0,4,0,0" FontSize="12" Opacity=".6" Text="About 707 MiB. Downloaded from NVIDIA on Hugging Face and kept on this computer." TextWrapping="Wrap"/>
                <TextBlock Margin="0,6,0,0" FontSize="12" Foreground="{DynamicResource ColorAccent300}">
                  <Run Text="OpenMDW-1.1 model terms: "/><Hyperlink x:Name="ModelTermsLink" RequestNavigate="OpenModelTerms" ToolTip="Open the OpenMDW-1.1 license in your default browser">openmdw.ai/license/1-1</Hyperlink>
                </TextBlock>
              </StackPanel>
            </Border>
            <CheckBox x:Name="TermsCheckBox" Margin="0,12,0,0" Checked="TermsChanged" Unchecked="TermsChanged"><TextBlock Width="350" TextWrapping="Wrap" Text="I understand that this downloads a separate local model under OpenMDW-1.1 terms."/></CheckBox>
          </StackPanel>
          <ProgressBar x:Name="ProgressBar" Margin="0,12,0,0" Height="8" Minimum="0" Maximum="100" Visibility="Collapsed"/>
          <TextBlock x:Name="ProgressCaption" Margin="0,8,0,0" FontSize="12" Opacity=".55"/>
          <TextBlock x:Name="StorageNoticeText" Margin="0,12,0,0" FontSize="12" Opacity=".55" TextWrapping="Wrap"/>
          <TextBlock Margin="0,12,0,0" FontSize="12" Opacity=".55" Text="The model stays on this computer. Your voice is processed locally."/>
          <StackPanel Margin="0,22,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="DownloadButton" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" Content="Download model" IsEnabled="False" Click="Download"/>
            <Button x:Name="RetryButton" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" Content="Retry" Visibility="Collapsed" Click="Retry"/>
            <Button x:Name="CancelButton" Content="Cancel" Click="Cancel"/>
          </StackPanel>
        </StackPanel>
      </Border>
    </Grid>
  </Border>
</Window>
